"""Тесты к диагностической задаче.

Ваша часть работы: придумать минимум пять случаев и проверить решение
на каждом. Обычный вход — только один из них. Обязательно нужны границы:
пустой список, один элемент, повторяющиеся числа и случай, когда пары нет.

Один тест написан как образец, остальные допишите сами.
Когда начнёте решать, уберите строку с pytestmark ниже.
"""
import pytest

from solution import two_sum

pytestmark = pytest.mark.skip(reason="уберите эту строку, когда начнёте решать")


def test_обычный_случай():
    assert two_sum([2, 7, 11, 15], 9) == (0, 1)


# def test_пустой_список():
#     ...

# def test_один_элемент():
#     ...

# def test_повторяющиеся_числа():
#     ...

# def test_пары_нет():
#     ...
